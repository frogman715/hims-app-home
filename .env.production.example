# ============================================
# HANMARINE INTEGRATED MANAGEMENT SYSTEM (HIMS)
# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Replace all placeholder values with actual production credentials
# 3. NEVER commit .env.production to git (it's in .gitignore)
# 4. Ensure all secrets are at least 32 characters long
# 5. Use strong, randomly generated secrets for production
#
# To generate secure secrets, use:
# openssl rand -base64 32
# ============================================

# --------------------------------------------
# NODE ENVIRONMENT
# --------------------------------------------
NODE_ENV=production

# --------------------------------------------
# DATABASE CONFIGURATION
# --------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example: postgresql://hims_prod_user:SecurePassword123@localhost:5432/hims_prod?schema=public
DATABASE_URL="postgresql://hims_prod_user:YOUR_SECURE_PASSWORD@localhost:5432/hims_prod?schema=public"

# --------------------------------------------
# NEXTAUTH AUTHENTICATION
# --------------------------------------------
# NextAuth secret for JWT encryption (REQUIRED - min 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="YOUR_SECURE_NEXTAUTH_SECRET_HERE_MIN_32_CHARS"

# Production URL (use HTTPS in production)
NEXTAUTH_URL="https://app.hanmarine.co"

# --------------------------------------------
# HIMS ENCRYPTION KEY
# --------------------------------------------
# AES-256-GCM encryption key for RED sensitivity data (REQUIRED - min 32 characters)
# Generate with: openssl rand -base64 32
HIMS_CRYPTO_KEY="YOUR_SECURE_CRYPTO_KEY_HERE_MIN_32_CHARS"

# --------------------------------------------
# EMAIL SERVICE CONFIGURATION (Optional)
# --------------------------------------------
# Email provider: nodemailer, gmail, sendgrid, mailgun, or aws_ses
EMAIL_PROVIDER="nodemailer"

# SMTP Configuration (if using EMAIL_PROVIDER=smtp)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@hanmarine.co"
SMTP_PASS="your-app-specific-password"

# SendGrid Configuration (if using EMAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY="SG.your-sendgrid-api-key"

# Mailgun Configuration (if using EMAIL_PROVIDER=mailgun)
# MAILGUN_API_KEY="your-mailgun-api-key"
# MAILGUN_DOMAIN="mg.hanmarine.co"

# AWS SES Configuration (if using EMAIL_PROVIDER=aws-ses)
# AWS_REGION="ap-southeast-1"
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"

# Email sender configuration
EMAIL_FROM="noreply@hanmarine.co"
EMAIL_USER="your-email@hanmarine.co"
EMAIL_PASSWORD="your-app-specific-password"
EMAIL_SERVICE="gmail"

# --------------------------------------------
# COMPANY INFORMATION
# --------------------------------------------
# Used in contracts, reports, and official documents
COMPANY_NAME="PT Hanmarine Mandiri Nusantara"
COMPANY_ADDRESS="Your Company Address, City, Country"
COMPANY_PHONE="+62-XXX-XXXX-XXXX"
COMPANY_EMAIL="info@hanmarine.co"
COMPANY_LICENSE_NUMBER="YOUR-LICENSE-NUMBER"

# --------------------------------------------
# FILE UPLOADS
# --------------------------------------------
# Base directory for storing uploaded files (absolute path recommended for VPS)
# VPS Production: /home/hanmarine/seafarers_files
# Development: /path/to/your/dev/uploads or relative path like public/uploads
UPLOAD_BASE_DIR="/home/hanmarine/seafarers_files"

# Maximum file size for uploads in MB (default: 20MB)
UPLOAD_MAX_SIZE_MB="20"

# Legacy upload directory (deprecated, use UPLOAD_BASE_DIR instead)
# UPLOADS_DIR="public/uploads"

# --------------------------------------------
# SCHEDULER & AUTOMATION (Optional)
# --------------------------------------------
# Secret token for scheduled task endpoints (min 32 characters)
# Generate with: openssl rand -base64 32
SCHEDULER_SECRET_TOKEN="YOUR_SCHEDULER_SECRET_TOKEN_HERE"

# --------------------------------------------
# SEEDING & DEVELOPMENT (Do not use in production deployment)
# --------------------------------------------
# Secret key for database seeding operations in non-production environments only
# SEED_SECRET_KEY="YOUR_SEED_SECRET_KEY_HERE"

# --------------------------------------------
# NEXT.JS SERVER ACTIONS (Required in Production)
# --------------------------------------------
# Encryption key for Next.js Server Actions (REQUIRED - min 32 characters)
# Generate with: openssl rand -base64 32
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY="YOUR_SERVER_ACTIONS_KEY_HERE_MIN_32_CHARS"

# ============================================
# PRODUCTION SECURITY CHECKLIST
# ============================================
# Before deploying to production, ensure:
# 
# ✅ All placeholder values replaced with actual credentials
# ✅ DATABASE_URL points to production PostgreSQL database
# ✅ NEXTAUTH_SECRET is at least 32 characters (use openssl rand -base64 32)
# ✅ HIMS_CRYPTO_KEY is at least 32 characters (use openssl rand -base64 32)
# ✅ NEXT_SERVER_ACTIONS_ENCRYPTION_KEY is at least 32 characters (use openssl rand -base64 32)
# ✅ NEXTAUTH_URL uses HTTPS protocol (https://app.hanmarine.co)
# ✅ Email credentials are valid and tested
# ✅ Company information is accurate
# ✅ File paths are correctly set for production server
# ✅ .env.production is NOT committed to git
# ✅ SSL certificates are installed and valid
# ✅ Database backups are configured
# ✅ Firewall rules are configured properly
# ✅ Run 'npx prisma migrate deploy' to apply database migrations
# ✅ Create production users manually via approved operational process
# ============================================
